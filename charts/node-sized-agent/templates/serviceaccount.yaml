{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "node-sized-agent.serviceAccountName" . }}
  labels:
    {{- include "node-sized-agent.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
